import React from 'react';

import World from '../World';

import './index.css';

const Core = () => {
    return (
        <div className='core'>
            <World />
        </div>
    );
};

export default Core;
